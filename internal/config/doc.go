// Package config defines the application configuration types and their default values.
// Configuration is driven by environment variables (see the env sub-package) and
// assembled into preset variables consumed by the command entrypoints.
package config
